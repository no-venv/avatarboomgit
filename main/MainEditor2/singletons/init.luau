return {
	catalog_fetcher = require(script.CatalogFetcher);
	humanoid_description = require(script.SimpleHumanoidDescription);
	event_bus = require(script.event_bus);
	wearing_cache = require(script.WearingCache);
	plugin_fs = require(script.PluginFS);
}
return {
	stroke = require(script.stroke);
	padding = require(script.padding);
	corner = require(script.corner);
	font = require(script.font);
	grid = require(script.grid);
	vbox = require(script.vbox);
	hbox = require(script.hbox);
	sizeconst = require(script.sizeconst)
}